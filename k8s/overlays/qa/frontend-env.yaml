apiVersion: apps/v1
kind: Deployment
metadata:
  name: clock-frontend
spec:
  template:
    spec:
      containers:
        - name: frontend
          env:
            - name: REACT_APP_ENV
              value: "qa"
